{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.mjs"
  },
  "dependencies": {
    "@effect/platform-node": "4.0.0-beta.78",
    "@price-monitor/api": "workspace:*",
    "@price-monitor/db": "workspace:*",
    "dotenv": "catalog:",
    "effect": "4.0.0-beta.78"
  },
  "devDependencies": {
    "@price-monitor/config": "workspace:*",
    "@types/node": "catalog:",
    "tsdown": "^0.21.9",
    "tsx": "^4.19.2",
    "typescript": "catalog:"
  }
}
